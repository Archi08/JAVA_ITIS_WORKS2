1) FileOutputStream outputStream =
                    new FileOutputStream(fileName, true <-- ?);

2) return user.getId(); -- почему возвращаем Id

3)  return -1; -- ?

4) split(); --  пон€л, что он разбивает сроку на части. ќн делает пробелы между словами? ≈сли это так, то когда выполн€етс€ это String currentUserData = reader.readLine(); - в переменную currentUserData закидываетс€ строка без пробелов?

5) currentUserData = reader.readLine(); <-- дл€ исключений?

6) ƒл€ чего нужен UserService 